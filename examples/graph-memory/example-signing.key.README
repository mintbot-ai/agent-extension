This private key exists ONLY so `axp verify examples/graph-memory/agent-extension.json` works out of the box. It is public by definition — never trust anything signed with it outside this repo, and never reuse it.
